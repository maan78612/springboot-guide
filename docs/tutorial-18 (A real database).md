# Tutorial 18 — A real database

Use Flyway with PostgreSQL instead of in-memory schema generation.

Files for this stage:

- New: `src/main/resources/db/migration/V1__init.sql`
- New: `src/main/resources/db/migration/V2__seed_catalog.sql`
- Updated: `pom.xml`
- New: `compose.yaml`
- Updated: `src/main/resources/application.properties`

---

## 1. Replace create-drop with Flyway

```properties
spring.jpa.hibernate.ddl-auto=validate
```

Flyway now owns the schema, and Hibernate checks it instead of creating it.

## 2. Add the migration scripts

```sql
CREATE TABLE book (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    title VARCHAR(255),
    author_id BIGINT,
    price DECIMAL(10,2)
);
```

This keeps the database structure versioned and repeatable.

## 3. Use Postgres for real deployments

```bash
docker compose up -d
```

Then point the app at PostgreSQL via environment variables.

Next: [**Tutorial 19 — Production basics**](tutorial-19%20%28Production%20basics%29.md)
